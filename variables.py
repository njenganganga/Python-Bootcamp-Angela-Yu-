# print(len(input("What is your name?")))
# 2 variables username(store name) and length(calculate len and stores the value)
username = input("Enter your name: ")
length = len(username)
print(length)
print(username[length-6]) # Used to also display the letter specified in the number. -1 is the far right number moving toward the left.

# ctrl + backward slash is used to quickly comment an uncomment a whole line of  code.